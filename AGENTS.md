# Dateflow 작업 지침

이 파일은 프로젝트 전체에 적용한다. 사용자의 최신 요청을 우선하며, 아래 기준을 이후 기능 개발과 UI 변경에도 일관되게 적용한다.

## 제품과 범위

- 서울에서 사용자가 선택한 장소 하나를 반드시 포함하는 데이트 코스를 만든다.
- 핵심 흐름은 장소 검색 → 조건 입력 → 지도·코스 시간표 확인 → 장소 일부 교체 → 저장·공유다. 채팅은 기본 인터페이스가 아니다.
- 이동 수단은 도보와 대중교통이다. 추천 범위는 행정구 경계보다 실제 이동 시간으로 판단한다.
- 기본 코스는 3~4개 장소로 구성한다. 예약 등 고정 일정이 있으면 그 시간과 장소를 보존한다.
- 비회원도 검색과 코스 생성을 이용할 수 있고, 저장할 때 로그인한다. 첫 로그인 제공자는 카카오다.
- 날짜와 시간은 Asia/Seoul 기준, 예산은 두 사람 합계 원화 기준으로 명확하게 표시한다.
- 현재 준비 화면을 완성된 제품 디자인이나 기능으로 간주하지 않는다. 구현 범위 밖의 기능을 임의로 추가하지 않는다.

## UI 디자인 원칙 — 필수

사용자는 템플릿을 조립한 듯한 '바이브코딩 느낌'을 원하지 않는다. 실제 제품 디자이너가 정보 구조와 상호작용까지 설계한 수준을 목표로 한다. 단순히 색상을 바꾸거나 장식을 추가하는 것으로 이 요구를 충족했다고 판단하지 않는다.

### 디자인 방향

- 서울의 장소를 발견하고 함께 보낼 시간을 계획하는 서비스에 맞춰, 차분하면서도 개성이 있는 도시 생활·문화 서비스의 시각 언어를 사용한다. 과도한 하트, 분홍색, 커플 일러스트에 기대지 않는다.
- 새 화면 작업 전에 주된 사용자 행동, 정보 우선순위, 레이아웃, 타이포그래피, 색상·여백 규칙을 정한다. 기존에 확립된 방향이 있으면 이어받는다.
- 첫 화면에서 장소 검색 등 실제 작업을 시작할 수 있게 한다. 거대한 슬로건이나 기능 홍보 카드가 핵심 작업을 밀어내지 않게 한다.
- 주요 행동은 화면 또는 작업 영역마다 분명하게 구분한다. 중요도가 다른 버튼을 같은 강조 수준으로 표시하지 않는다.
- 장소 이름·사진·이동 시간·방문 순서처럼 의사결정에 필요한 정보를 시각적 중심에 둔다. 장식보다 콘텐츠가 화면의 인상을 만든다.

### 피해야 할 관성적인 패턴

- 이유 없는 보라·파랑 그라데이션, 빛 번짐, 유리 효과, 떠다니는 도형, 이모지 아이콘을 사용하지 않는다.
- 모든 정보를 둥근 카드에 넣거나, 같은 크기의 카드 3개를 반복하거나, 의미 없이 배지·아이콘·그림자를 붙이지 않는다. 목록·구분선·여백·타이포그래피로 먼저 구조를 만든다.
- shadcn/ui의 기본 테마나 예제 레이아웃을 완성 디자인으로 사용하지 않는다. 접근성과 동작 기반으로 활용하고 Dateflow의 디자인 규칙으로 스타일링한다.
- '특별한 경험', '완벽한 데이트', 'AI가 추천하는' 같은 근거 없는 홍보 문구와 불필요한 영문 라벨을 피한다. 사용자가 판단하거나 행동하는 데 필요한 한국어를 쓴다.
- 참고 디자인이 주어지면 표면적인 색상만 복제하지 말고 정보 밀도·여백·글자 위계·상호작용을 분석해 적용한다.

### 구체적인 완성 기준

- 공통 색상·간격·글자 크기·모서리·그림자는 토큰으로 관리한다. 기존 `src/app/globals.css`를 먼저 확장하고 화면마다 임의 값을 늘리지 않는다.
- 한글 줄바꿈, 제목 길이, 숫자와 단위의 정렬을 확인한다. 본문은 기본 16px 이상, 반복해서 읽는 레이블은 기본 14px 이상을 사용한다.
- 데스크톱은 코스와 지도를 함께 비교할 수 있게, 모바일은 작은 지도·목록 전환 또는 상세 패널로 핵심 작업을 유지하도록 설계한다. 단순히 데스크톱을 축소하지 않는다.
- 장소 사진은 실제 장소와 일치하고 사용 가능한 출처의 이미지만 사용한다. 다른 장소나 생성 이미지를 실제 장소 사진처럼 제시하지 않는다.
- 로딩·빈 결과·오류·선택·저장 완료·긴 장소명 상태를 고려한다. 동작하지 않는 버튼을 완성 기능처럼 두지 않는다.
- 키보드 탐색, 명시적 입력 레이블, 초점 표시, 충분한 명암 대비와 터치 영역을 제공한다. 색상만으로 상태를 구분하지 않는다.
- 애니메이션은 선택·전환·결과 갱신을 이해하는 데 도움이 될 때만 짧게 적용하고 `prefers-reduced-motion`을 존중한다.
- UI 변경 완료 전 좁은 모바일 화면과 데스크톱, 긴 텍스트, 오류·빈 결과 상태를 검토한다. 브라우저 검증을 실제로 했는지와 코드 수준에서만 확인했는지를 구분해 보고한다.

## 데이터와 코스 생성

- 코스 생성의 주체는 서버의 규칙 기반 로직이다. 장소·경로 API는 입력 데이터를 제공한다. 유료 생성형 AI API는 기본 구성에 포함하지 않는다.
- 조건 필터링 → 후보 점수 계산 → 조합 → 전체 일정 검증 순서로 구현하고 추천 근거를 설명할 수 있게 한다.
- 실제 이동 시간, 환승, 체류 시간, 식사 시간, 예산, 활동 중복과 되돌아가는 동선을 고려한다.
- 장소 교체 시 사용자가 고정한 장소를 유지하고 관련 이동 구간과 이후 일정을 다시 계산한다.
- 가격·영업시간·분위기·예약 가능 여부가 없으면 추측하지 않는다. 확인되지 않은 정보와 추정 시간·비용을 분명히 구분한다.
- 예시 데이터는 예시라고 표시한다. 목업 결과를 실제 API 추천이나 저장 성공으로 표현하지 않는다.
- 공급자별 호출 제한·이용 조건·저장 허용 범위는 연동 시 공식 문서로 확인한다.

## 비용과 보안

- 초기 예산은 0원이다. 유료 플랜, 유료 추가 쿼터, 도메인 구매 등을 임의로 활성화하지 않는다.
- 사용자당 일일 생성 제한은 기본적으로 두지 않는다. 중복 요청 방지, 짧은 시간 과다 요청 차단, 공급자 무료 한도 소진 시 중단은 서버에서 구현한다.
- API 호출 수를 코스 생성 단위로 관리하고 후보를 좁힌 후 경로를 조회한다. 클라이언트 제한만으로 비용을 보호하지 않는다.
- 외부 API 키는 `.env.local`에 두고 `.env.example`에는 빈 값과 설명만 남긴다. 비밀 값을 로그·화면·저장소에 노출하지 않는다.
- 공개용 키와 서버 전용 키를 구분한다. Supabase 관리자 키 및 카카오 REST 키를 `NEXT_PUBLIC_` 변수에 넣지 않는다.
- 사용자 데이터 테이블을 만들 때 RLS와 소유자 접근 정책을 함께 작성한다. 로그인 화면만으로 데이터 접근이 보호된다고 간주하지 않는다.
- 인증 콜백과 세션 갱신, 서버 인증 검증을 함께 구현한다. 공유 링크에는 공개하기로 한 코스 정보만 포함한다.

## 개발과 검증

- Next.js App Router, TypeScript strict, Tailwind CSS, Supabase 구성을 유지한다. 패키지 관리자는 pnpm이며 `packageManager`에 지정된 버전과 `pnpm-lock.yaml`을 함께 관리한다. npm·yarn 잠금 파일을 생성하지 않는다.
- 경로 별칭은 `@/*` → `src/*`다. 서버 전용 로직에는 `server-only`를 사용하고 클라이언트 컴포넌트는 상호작용이 필요한 범위로 제한한다.
- UI, 외부 API 호출, 코스 생성 로직을 분리한다. 화면 파일에 추천 알고리즘이나 공급자 응답 변환을 몰아넣지 않는다.
- 작업과 무관한 리팩터링이나 패키지 추가를 피한다. 기존 사용자 변경을 덮어쓰지 않는다.
- 코드 변경 후 `pnpm check`, 기능·설정 변경 후 필요에 따라 `pnpm build`를 실행한다. 문서만 바꾼 경우 불필요한 빌드는 생략한다.
- 추천 조건, 시간 계산, 권한처럼 실패 영향이 있는 동작에는 의미 있는 테스트를 추가한다. 단순 정적 문구·스타일 변경에 구현을 그대로 반복하는 테스트는 추가하지 않는다.
- 외부 계정이나 키가 없으면 실행 가능한 작업을 먼저 완료하고 연결이 필요한 부분을 정확하게 기록한다. 성공하지 않은 연동을 완료로 보고하지 않는다.
- 사용자가 커밋을 요청하면 `<type>: <간결한 한국어 설명>` 형식으로 작성한다.

## Next.js 자동 생성 지침

아래 블록은 Next.js가 관리한다. 프로젝트 지침은 블록 밖에 유지한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
